// import { fromJS } from 'immutable';
// import { makeSelectEntityQueryDomain } from '../selectors';

// const selector = makeSelectEntityQueryDomain();

describe('makeSelectEntityQueryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
