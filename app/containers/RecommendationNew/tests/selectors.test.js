// import { fromJS } from 'immutable';
// import { makeSelectRecommendationNewDomain } from '../selectors';

// const selector = makeSelectRecommendationNewDomain();

describe('makeSelectRecommendationNewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
