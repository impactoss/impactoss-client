// import { fromJS } from 'immutable';
// import { makeSelectActionNewDomain } from '../selectors';

// const selector = makeSelectActionNewDomain();

describe('makeSelectActionNewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
