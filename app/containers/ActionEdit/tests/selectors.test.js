// import { fromJS } from 'immutable';
// import { makeSelectActionEditDomain } from '../selectors';

// const selector = makeSelectActionEditDomain();

describe('makeSelectActionEditDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
