// import { fromJS } from 'immutable';
// import { makeSelectActionViewDomain } from '../selectors';

// const selector = makeSelectActionViewDomain();

describe('makeSelectActionViewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
