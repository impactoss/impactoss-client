// import { fromJS } from 'immutable';
// import { makeSelectRegisterUserPageDomain } from '../selectors';

// const selector = makeSelectRegisterUserPageDomain();

describe('makeSelectRegisterUserPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
