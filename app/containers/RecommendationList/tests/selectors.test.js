// import { fromJS } from 'immutable';
// import { makeSelectActionListDomain } from '../selectors';

// const selector = makeSelectActionListDomain();

describe('makeSelectActionListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
