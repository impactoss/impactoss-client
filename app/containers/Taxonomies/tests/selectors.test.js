// import { fromJS } from 'immutable';
// import { makeSelectTaxonomiesDomain } from '../selectors';

// const selector = makeSelectTaxonomiesDomain();

describe('makeSelectTaxonomiesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
