import styled from 'styled-components';
import { palette } from 'styled-theme';

const ReportDueDate = styled.div`
  color: ${palette('greyscaleDark', 4)};
  display: block;
`;

export default ReportDueDate;
