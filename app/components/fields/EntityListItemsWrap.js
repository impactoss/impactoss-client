import styled from 'styled-components';
import { palette } from 'styled-theme';

const EntityListItemsWrap = styled.div`
  border-bottom: 1px solid ${palette('greyscaleLight', 0)};
`;

export default EntityListItemsWrap;
