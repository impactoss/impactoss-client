import React, { PropTypes } from 'react';
import styled from 'styled-components';

export default class EntityListItemMainExpandable extends React.PureComponent { // eslint-disable-line react/prefer-stateless-function

}
