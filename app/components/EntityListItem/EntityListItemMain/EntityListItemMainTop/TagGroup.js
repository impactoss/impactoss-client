import styled from 'styled-components';

const BottomTagGroup = styled.span`
  display: inline;
  float: right;
  margin-left: 1em;
  margin-top: -4px;
  &:first-child: {
    margin-left: 0;
  }
`;

export default BottomTagGroup;
